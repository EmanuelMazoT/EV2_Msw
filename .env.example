# Ejemplo de variables de entorno. Copia este archivo a .env y completa los valores.
# La contrasena SIEMPRE debe ir en variables de entorno, nunca en archivos del repo.
DB_URL=
DB_USERNAME=
DB_PASSWORD=